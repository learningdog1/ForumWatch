# 更新日志（Changelog）

所有对用户可见的更新都记录在此。每个版本发布时，release 说明取自本文档对应段落
（由 `.github/workflows/release.yml` 自动完成——**发版前必须先在本文档顶部新增本版段落**，
否则 release job 会失败：宁可构建失败，不发没有说明的版本）。

格式约定：`## [vX.Y.Z] - YYYY-MM-DD`，新增/改进/修复分组，面向使用者而非开发者。

## [v0.9.0] - 2026-09-25

分类行情日报三连修：AI 总结缺失、推送不渲染 markdown、栏目挖不出价值。

### 修复

- **报告「没有 AI 总结」**：报告的 AI 调用此前没关「思考模式」——思考型模型把输出预算
  （max_tokens）吃光后返回空内容，报告就静默降级成纯统计模板照常推送，只在头注留一行不显眼的
  「模板模式」。现在命中日报与分类行情报告的 AI 调用全部走直出模式（与语义评估、锐评同款），
  输出上限同步放宽（分类报告 2000→4000、日报 1500→3000 tokens）。AI 真失败时仍降级统计模板
  （报告不断档），头注改为 ⚠️ 更醒目。

### 改进

- **推送渲染为富文本**：报告推送改为 Telegram HTML——标题加粗、表格等宽字体对齐（分隔行
  `| --- |` 不再裸奔）、引用块、列表符号；报告推送同时禁用链接预览卡片。Bark/ntfy/Webhook
  通道收到的是剥掉 markdown 记号的纯文本（表格同样对齐）。分段上限 3500→3000 给 HTML 膨胀
  留出 4096 余量，个别段转换后超限时自动退纯文本，绝不发送失败。正文里的裸 URL 在 HTML
  模式下显式链接化，纯文本通道保持 Telegram 原生可点。
- **分类报告升级为「行情简报」**：AI 产出固定五段结构——🧠 总评 / 🔥 热点（按分类归纳、引用
  真实标题）/ 📈 趋势 / 💰 行情解读（基于价格分位统计解读，不再凭标题猜；无样本如实写）/
  🔮 展望（下期观察点，注明推测）。AI 的输入同时带上了这些确定性统计，行情解读有真实分位数
  依据、且与报告统计段同源不两套口径。确定性栏目同步 emoji 化：📊 帖量分布、🔁 同帖多发与
  活跃作者、💸 价格分位、📋 覆盖率——一眼分栏。报告文件与推送结构一致。
- **AI 分段按 token 预算自适应（大上下文时代）**：旧实现按 60 帖/段固定切分、1800 帖封顶——
  679 帖/天的日报要串 13 次 AI 调用（任一段失败整体降级模板），月报直接撞上限、从来没有
  AI 总结。现在按单次调用 90k token 预算贪心打包（对 128k 上下文模型也安全，1M 模型绰绰
  有余）：679 帖/天一次调用完成，15000 帖/月约 14 段，上限提高到约数万帖；单次调用超时随
  载荷放大（60s + 1s/千 token，封顶 180s）。
- **AI 载荷带上 url 与摘要**（来源提供时摘要截 120 字）：🔥 热点条目可附代表帖的 markdown
  链接（url 原样取自数据、禁止编造），命中日报清单同样逐条可链接、降级模板行尾附裸链接——
  报告从「看个概览」变成「直接跳帖」。
- 命中监控日报的 AI 结构同步升级（📌 概览 / 📋 命中清单 / 💡 观察），并同样走直出模式。

### 内部

- `Notifier.sendRaw(text, opts?)` 增可选 HTML 形态（`RawMessageOptions`，接口向后兼容）；
  新增 `notify/markdown-report` 纯函数渲染器（markdown → Telegram HTML / 纯文本）。
  决策记录见 `docs/decisions.md` R19。

## [v0.8.1] - 2026-09-22

实现 [issue #2](https://github.com/learningdog1/ForumWatch/issues/2) 的两条建议。

### 新增

- **推送作者可点击直达个人主页**（建议一）：Telegram 命中推送里的「👤 作者」现在是超链接——
  NodeSeek 指向发帖人主页（`/space/{id}`，页内有私信入口）、V2EX 指向 `/member/{username}`。
  私有帖看不到正文也能知道是谁发的、一键联系对方。RSS 来源与升级前的历史记录没有该链接，
  作者照旧显示纯文本，其余消息内容不变。
- **命中词 `#` 标签**（建议二）：命中关键词与规则名统一带 `#` 前缀（如 `🎯 命中: #甲骨文, #免费`）。
  Telegram 会把 `#` 开头的词识别为话题标签，点击即在会话内筛选同标签消息——按触发词过滤推送流
  不用再肉眼扫。语义命中的理由是自然语言句子，不加前缀。
- Webhook 推送的 `topic` 载荷新增可选字段 `authorUrl`（来源提供作者主页时携带）。

### 内部

- `Topic` 数据契约新增可选字段 `authorUrl`（与 `excerpt` 同款「旧记录容忍缺失」约定）；
  NodeSeek 适配器从页面作者锚点解析，V2EX 按 username 构造。决策记录见 `docs/decisions.md` R18。

## [v0.8.0] - 2026-09-22

### 新增

- **NodeSeek 分类阶段报告（日 / 周 / 月报）**：不止盯着命中——引擎把观察到的每一个新帖
  （命中与否都算）落进本地全量话题存档（保留 35 天），按分类（默认情报 / 交易 / 测评）定时产出
  三档行情总结：AI 总述 + 分类热点矩阵 + 趋势（同帖多发 / 活跃作者）+ VPS 价格行情分位表。
  报告文件附全量帖子清单可核对防漏（推送不附，防月报刷屏）。AI 不可用时自动降级为纯统计模板，
  报告不断档。周报固定周一总结上一完整周、月报固定每月 1 日总结上一自然月。默认关闭，
  在「AI 设置 → 分类报告」开启。

## [v0.7.3] - 2026-09-21

线上事故复盘（AI 网关余额耗尽导致两天 97 条语义候选被静默丢弃）的四连修。

### 修复

- **AI 故障不再静默丢帖**：语义评估连续失败 ≥ 10 分钟即推送「⚠️ 语义评估已连续失败」告警
  （含错误原因），恢复时补发通知。余额耗尽这类应用修不了的问题，至少让你**知道**。
- **未决帖收口从「5 轮」改「时间窗」**（默认 30 分钟，可配）：快轮询下轮数口径几秒钟就把
  在途帖判死；时间窗内持续重评，超窗才降级字面判定收口。
- **退避尊重上游 retry_after**：429 指示的等待时长直接生效（原先指数曲线可能短于上游要求），
  并修复同轮多批时冷却不生效、退避连升白烧调用的问题。
- **评估调用提速**：超时 15s → 30s；评估请求默认禁用推理型模型的思考输出（与锐评同款直出模式）。

### 新增

- **「任何模型」都能禁思考**：思考禁用参数一次下发三种方言（智谱 GLM / Qwen 系 / vLLM-SGLang），
  严格端点对未知参数回 400 时自动去参重试并记住该端点——换任何 OpenAI 兼容模型都能用。

## [v0.7.2] - 2026-09-21

### 新增

- **`&&` 组合关键词**：词条内用 `&&` 连多个词（如 `搬瓦工 && 香港`）表示须**同时命中**才推，
  「(A 且 B) 或 C」这类组合条件也能表达。

## [v0.7.1] - 2026-09-21

### 新增

- **Docker 部署**：headless Web UI 服务器模式（浏览器访问监控台，无需桌面环境）、
  官方 Docker 镜像与发布 CI。`docker compose up` 即可跑。

## [v0.7.0] - 2026-09-21

### 新增

- **来源级匹配覆盖**：每个来源可独立设置包含词 / 排除词 / 匹配模式 / 兴趣描述 / 语义阈值，
  未设置的项跟随全局。
- **来源级全匹配（matchAll）**：一键推送该来源全部新帖（LowEndTalk Offers 类场景），
  排除词与降噪等四道闸仍生效。
- **LowEndTalk Offers 预设**（并修正了坏预设地址 `/feed` → `/discussions/feed.rss`）。
- **语义评估退避**：连续失败进入指数退避冷却，冷却期零调用防重试风暴。

### 修复

- 单来源持续失败触发退避时，不再抬高全局轮询间隔（其余来源照常轮询）。

## [v0.6.0] - 2026-09-21

### 改进

- **AI 锐评直出模式**（默认开）：锐评请求禁用推理型模型的思考输出——更快、更稳
  （思考 token 吃光产出预算导致锐评为空的旧事故根因）；设置里保留开关。
- README 全面重写：截图、定位与工作原理图；深度语义文档迁移至 `docs/usage.md`。

## [v0.5.0] - 2026-09-20

### 新增

- **Telegram Bot 远程遥控**：`/status` `/pause` `/resume` `/poll` `/help` 随时随地掌控，
  会话允许清单硬闸。
- **凭据安全**：bot token / API key 经系统钥匙串（safeStorage）加密落盘。
- **运维加固**：引擎看门狗（卡死自动重启）、新版本更新检查、配置备份导出/导入、
  headless 配置热重载、Cloudflare 挑战的浏览器栈回退。
- **可观测性**：每帖每轮的 14 类出口去向全量入「流水」页；历史命中跨日检索 + 统计面板
  （哪些关键词从未命中该删了）；命中行 👍/👎 反馈，AI 越用越合口味。

---

v0.5.0 之前为密集开发期（多论坛引擎 / AI 语义匹配 / 多通道推送 / 桌面 UI 四大主线），
未发正式版本，细节见 git 历史与 `docs/decisions.md`。
